#include<stdio.h>

int main()
{
    char Ch;
    
	printf("Enter a character:");
	scanf("%c",&ch);
	if(ch>='a' && c<='z')
printf("is an small.");
 else if(Ch>='A' && Ch<='Z')

		printf(" is an capital.");
	else if(ch>='0' && ch<='9')
		printf("is a digit.");
	else
		printf("is a special character.");
	return 0;

    
}